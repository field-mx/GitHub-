**0.安装conda并激活base环境**
```bash
# 1. 下载 Miniconda 脚本
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh

# 2. 静默安装到用户目录下的 miniconda3 文件夹
bash miniconda.sh -b -u -p ~/miniconda3

# 3. 删除安装脚本释放空间
rm miniconda.sh

# 4. 初始化 bash 环境
~/miniconda3/bin/conda init bash

# 5. 刷新终端配置使其立即生效
source ~/.bashrc

```
**1.创建新环境**
```bash
# 1.创建新环境
conda create -n PROJECT NAME python=3.10 -y

# 2. 激活环境
conda activate mac_net

# 3. 升级 pip 到最新版（很重要，旧版 pip 经常无法正确解析 wheel 依赖）
python -m pip install --upgrade pip
```
**2.查看cuda版本**
```bash
nvidia-smi
```
**3.安装对应版本的cuda+torch组合，以及mamba需要的编译工具**
```bash
pip install torch==2.1.2 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
conda install -c "nvidia/label/cuda-12.1.0" cuda-toolkit -y
pip install packaging ninja triton
pip install causal-conv1d>=1.2.0
pip install mamba-ssm
```
